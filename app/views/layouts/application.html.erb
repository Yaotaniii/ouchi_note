<!DOCTYPE html>
<html>
  <head>
    <title>おうちノート</title>
    <link rel="icon" href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'><text y='.9em' font-size='90'>🏠</text></svg>">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <% if user_signed_in? %>
      <header class="header">
        <h1>🏠 おうちノート</h1>
        <nav class="header-nav">
          <%= link_to "ダッシュボード", dashboard_path %>

          <div class="dropdown">
            <span class="dropdown-toggle">物件管理</span>
            <div class="dropdown-menu">
              <%= link_to "部屋一覧", rooms_path %>
              <%= link_to "入居者一覧", residents_path %>
              <%= link_to "入金管理", payments_path %>
              <%= link_to "修繕履歴", maintenance_records_path %>
            </div>
          </div>

          <div class="dropdown">
            <span class="dropdown-toggle">駐車・駐輪</span>
            <div class="dropdown-menu">
              <%= link_to "駐車場", parking_spaces_path %>
              <%= link_to "駐輪場", bicycle_registrations_path %>
              <%= link_to "バイク置場", motorcycle_registrations_path %>
              <%= link_to "車両情報", vehicles_path %>
            </div>
          </div>

          <%= link_to "対応履歴", incidents_path %>
          <%= link_to "使い方", help_path %>

          <div class="dropdown">
            <span class="dropdown-toggle"><%= current_user.name %>さん</span>
            <div class="dropdown-menu dropdown-menu-right">
              <span class="dropdown-item-text"><%= current_user.owner? ? 'オーナー' : 'スタッフ' %></span>
              <% if current_user.owner? %>
                <%= link_to "ユーザー管理", users_path %>
              <% end %>
              <%= link_to "ログアウト", destroy_user_session_path, data: { turbo_method: :delete } %>
            </div>
          </div>
        </nav>
      </header>

      <main class="container">
        <% if notice %>
          <div class="flash flash-notice"><%= notice %></div>
        <% end %>
        <% if alert %>
          <div class="flash flash-alert"><%= alert %></div>
        <% end %>

        <%= yield %>
      </main>
    <% else %>
      <% if notice %>
        <div class="flash flash-notice" style="position: fixed; top: 20px; left: 50%; transform: translateX(-50%); z-index: 1000;"><%= notice %></div>
      <% end %>
      <% if alert %>
        <div class="flash flash-alert" style="position: fixed; top: 20px; left: 50%; transform: translateX(-50%); z-index: 1000;"><%= alert %></div>
      <% end %>

      <%= yield %>
    <% end %>
  </body>
</html>