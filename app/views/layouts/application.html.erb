<!DOCTYPE html>
<html>
  <head>
    <title>おうちノート</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <% if user_signed_in? %>
      <header class="header">
        <h1>🏠 おうちノート</h1>
        <nav class="header-nav">
          <span class="user-info"><%= current_user.name %>さん（<%= current_user.owner? ? 'オーナー' : 'スタッフ' %>）</span>
          <%= link_to "ダッシュボード", dashboard_path %>
          <%= link_to "部屋一覧", rooms_path %>
          <%= link_to "住民一覧", residents_path %>
          <%= link_to "入金管理", payments_path %>
          <%= link_to "修繕履歴", maintenance_records_path %>
          <%= link_to "駐車場", parking_spaces_path %>
          <%= link_to "ログアウト", destroy_user_session_path, data: { turbo_method: :delete } %>
        </nav>
      </header>
    <% end %>

    <main class="container">
      <% if notice %>
        <div class="flash flash-notice"><%= notice %></div>
      <% end %>
      <% if alert %>
        <div class="flash flash-alert"><%= alert %></div>
      <% end %>

      <%= yield %>
    </main>
  </body>
</html>